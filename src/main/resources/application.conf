ktor {
    deployment {
        port = ${?PORT}
        port = 8080
    }
    application {
        modules = [ LoreBible.com.lorebible.ApplicationKt.module ]
    }
}

jwt{
    secret = ${jwtSecret}
    issuer = ${jwtIssuer}
    audience = ${jwtAudience}
}
