ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ LoreBible.com.lorebible.ApplicationKt.module ]
    }
}

jwt{
    secret = ${jwtSecret}
    issuer = ${jwtIssuer}
    audience = ${jwtAudience}
}
