ktor {
    deployment {
        port = ${?PORT}
        port = 8080
    }
    application {
        modules = [ com.lorebible.ApplicationKt.module ]
    }
}

jwt {
    secret = ${JWT_SECRET}
    issuer = ${JWT_ISSUER}
    audience = ${JWT_AUDIENCE}
    expiration = ${JWT_EXPIRATION}
}
